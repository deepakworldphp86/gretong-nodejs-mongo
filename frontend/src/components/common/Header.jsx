import React from "react";
import layOutCss from '../../public/styles/home.module.css'


const Header = (home) => {
  return (

    <header className={layOutCss.header}>
    </header>
  );
};
export default Header;